import React from "react";
import ContactUserCard from "./ContactUserCard";

const Contacts = () => {
  return (
    <div>
        <h2>Contacts</h2>
      <ContactUserCard />
    </div>
  );
};

export default Contacts;