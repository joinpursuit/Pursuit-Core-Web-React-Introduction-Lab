import React from 'react'
import ContactUserCard from './ContactUserCard'

const Contacts = () =>{
    return(
        <div>
            < ContactUserCard />
            < ContactUserCard />
            < ContactUserCard />
            < ContactUserCard />
        </div>

    )
}

export default Contacts